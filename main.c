#include <stdio.h>
#include <stdlib.h>

#include "include/data-structures/queue.h"


int main(void)
{
  Queue q = createQueue(5);

  printf("%i\n", 5 != 5);

  // printf("The min value in the tree is: %i \n", tree_get_min_val(t));
}
